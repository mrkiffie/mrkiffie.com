<!DOCTYPE html>
<html lang="{{site.locale}}">
{{>html-head}}

<body>
    {{>header}}

    <main role="main">

        <article>
            {{{contents}}}
        </article>

    </main>

    {{>footer}}
</body>
</html>
